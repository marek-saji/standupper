mixin SvgImage(href)
  svg
    image(xlink:href='/images/' + href + '.svg', src='/images/' + href + '.png', width='100%', height='100%')

h2 +SvgImage
  p SVG image with PNG fallback. Width and height have to be set by CSS.

  +SvgImage()
